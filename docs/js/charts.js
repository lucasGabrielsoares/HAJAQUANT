let equityChart=null,ddChart=null;const chartOptions={responsive:true,maintainAspectRatio:false,interaction:{intersect:false,mode:'index'},plugins:{legend:{display:false},tooltip:{backgroundColor:'#111925',borderColor:'#29364a',borderWidth:1,titleColor:'#fff',bodyColor:'#aeb9c9'}},scales:{x:{grid:{display:false},ticks:{color:'#657288',maxTicksLimit:9}},y:{grid:{color:'#1c2635'},ticks:{color:'#657288'}}}};export function renderCharts(metrics){const eq=metrics.equity||[];const dd=metrics.ddSeries||[];document.getElementById('equityEmpty').style.display=eq.length?'none':'grid';document.getElementById('ddEmpty').style.display=dd.length?'none':'grid';if(equityChart)equityChart.destroy();if(ddChart)ddChart.destroy();if(!eq.length||typeof Chart==='undefined')return;equityChart=new Chart(document.getElementById('equityChart'),{type:'line',data:{labels:eq.map((_,i)=>i),datasets:[{data:eq,borderWidth:2,pointRadius:0,tension:.18,fill:true,backgroundColor:'rgba(79,140,255,.08)',borderColor:'#4f8cff'}]},options:chartOptions});ddChart=new Chart(document.getElementById('ddChart'),{type:'line',data:{labels:dd.map((_,i)=>i),datasets:[{data:dd.map(x=>x.dd),borderWidth:2,pointRadius:0,tension:.18,fill:true,backgroundColor:'rgba(255,100,124,.07)',borderColor:'#ff647c'}]},options:{...chartOptions,scales:{...chartOptions.scales,y:{...chartOptions.scales.y,ticks:{color:'#657288',callback:v=>v.toLocaleString('pt-BR')}}}}})}