name: Sync GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keep Pages build independent
        run: echo "GitHub Pages files are maintained directly in docs/"
